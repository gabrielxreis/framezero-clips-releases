# V81J — pasta simples + legendas unificadas

Alterações:

- Cada corte continua tendo uma pasta com o título correto.
- As versões `16x9` e `9x16` agora ficam na mesma pasta do corte, sem subpastas extras.
- O nome dos vídeos continua usando o título do corte:
  - `Título do Corte - 16x9.mp4`
  - `Título do Corte - 9x16.mp4`
- `legenda-instagram.txt` e `legenda-tiktok.txt` foram unificados em `legendas-redes-sociais.txt`.
- `legenda.ass` foi removido da geração para deixar a pasta mais limpa.
- `legenda.srt`, `resumo-do-corte.txt` e `metadata.json` continuam sendo gerados.
